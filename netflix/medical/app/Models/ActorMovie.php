<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class ActorMovie extends Model
{
    // Specify the table name if it differs from the model name
    protected $table = 'actor_movie';

    // Define the relationship between the ActorMovie model and other models (e.g., Actor and Movie)
    // ...

    // Add any additional model logic or customization as needed
}
